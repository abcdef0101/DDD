'use strict';

const config = {
    db: {
            host: '127.0.0.1',
            port: 5432,
            database: 'example',
            user: 'marcus',
            password: 'marcus',
        },
    transport: 'ws',
};

module.exports = config;